# 开发记录001

## 修改游戏启动分辨率

窗口模式和分辨率由 UGameUserSettings 控制，UGameUserSettings 中的属性可以通过 Config 配置

```c++
// GameUserSettings.h

/**
 * Stores user settings for a game (for example graphics and sound settings), with the ability to save and load to and from a file.
 */
UCLASS(config=GameUserSettings, configdonotcheckdefaults, MinimalAPI)
class UGameUserSettings : public UObject
{
    // ...
    
	/** Game screen resolution width, in pixels. */
	UPROPERTY(config)
	uint32 ResolutionSizeX;

	/** Game screen resolution height, in pixels. */
	UPROPERTY(config)
	uint32 ResolutionSizeY;

	/**
	 * Game window fullscreen mode
	 *	0 = Fullscreen
	 *	1 = Windowed fullscreen
	 *	2 = Windowed
	 */
	UPROPERTY(config)
	int32 FullscreenMode;
    
    // ...    
}
   
```

> Config 配置逻辑: [Configuration Files in Unreal Engine | Unreal Engine 5.7 Documentation | Epic Developer Community](https://dev.epicgames.com/documentation/unreal-engine/configuration-files-in-unreal-engine)

创建 DefaultGameUserSettings.ini 配置文件

```ini
[/Script/Engine.GameUserSettings]
bUseVSync=False
ResolutionSizeX=600
ResolutionSizeY=800
FullscreenMode=2
Version=5
FrameRateLimit=60

```

## 相机固定，仅移动玩家

MovementComponent 移动 Pawn 时实际是移动 UpdateComponent

```c++
// MovementComponent.h

/**
 * The component we move and update.
 * If this is null at startup and bAutoRegisterUpdatedComponent is true, the owning Actor's root component will automatically be set as our UpdatedComponent at startup.
 * @see bAutoRegisterUpdatedComponent, SetUpdatedComponent(), UpdatedPrimitive
 */
UPROPERTY(BlueprintReadOnly, Transient, DuplicateTransient, Category=MovementComponent)
TObjectPtr<USceneComponent> UpdatedComponent;
```

UpdateComponent 默认为 Owner 的 RootComponent

```c++
// MovementComponent.cpp

void UMovementComponent::InitializeComponent()
{
	TGuardValue<bool> InInitializeComponentGuard(bInInitializeComponent, true);
	Super::InitializeComponent();

	// RootComponent is null in OnRegister for blueprint (non-native) root components.
	if (bAutoRegisterUpdatedComponent && !IsValid(UpdatedComponent))
	{
		// Auto-register owner's root component if found.
		if (AActor* MyActor = GetOwner())
		{
			if (USceneComponent* NewUpdatedComponent = MyActor->GetRootComponent())
			{
				SetUpdatedComponent(NewUpdatedComponent);
			}
		}
	}

	if (bSnapToPlaneAtStart)
	{
		SnapUpdatedComponentToPlane();
	}
}
```

具体的移动逻辑参考下面文章
> - [UE移动解析](https://zhuanlan.zhihu.com/p/162770699)

所以实现“**相机固定，仅移动玩家**”只需要将相机组件和需要移动的组件分离，然后修改 UpdateComponent

```c++
// MovementComponent.h

/** Assign the component we move and update. */
UFUNCTION(BlueprintCallable, Category="Components|Movement")
ENGINE_API virtual void SetUpdatedComponent(USceneComponent* NewUpdatedComponent);
```

### 遇到的问题

- 不能将相机组件放到 PlayerController 中，显示存在问题

---

搞复杂了，直接再场景中放一个 CameraActor，设置为当前 PlayerController 的 ViewTarget。

```c++
// PlayerController.h

/**
 * Set the view target blending with variable control
 * @param NewViewTarget - new actor to set as view target
 * @param BlendTime - time taken to blend
 * @param BlendFunc - Cubic, Linear etc functions for blending
 * @param BlendExp -  Exponent, used by certain blend functions to control the shape of the curve. 
 * @param bLockOutgoing - If true, lock outgoing viewtarget to last frame's camera position for the remainder of the blend.
 */
UFUNCTION(BlueprintCallable, Category="Game|Player", meta=(Keywords = "Camera"))
ENGINE_API virtual void SetViewTargetWithBlend(class AActor* NewViewTarget, float BlendTime = 0, enum EViewTargetBlendFunction BlendFunc = VTBlend_Linear, float BlendExp = 0, bool bLockOutgoing = false);
```

然后关闭 PlayerController 的 bAutoManageActiveCameraTarget 选项

```c++
// PlayerController.h

/** 
 * True to allow this player controller to manage the camera target for you,
 * typically by using the possessed pawn as the camera target. Set to false
 * if you want to manually control the camera target.
 */
UPROPERTY(EditAnywhere, Category=PlayerController)
bool bAutoManageActiveCameraTarget;
```

## Sprite 移动过程中存在闪烁

修改项目抗锯齿方法（Anti-Aliasing Method）

FXAA/SMAA 都还好， TSR 和 TAA 会有闪烁和拖影

关闭抗锯齿也能解决闪烁问题

>  [Anti Aliasing and Upscaling in Unreal Engine | Unreal Engine 5.7 Documentation | Epic Developer Community](https://dev.epicgames.com/documentation/unreal-engine/anti-aliasing-and-upscaling-in-unreal-engine)

## UFloatingPawnMovement 的移动无法同步到服务器

临时解决方案：再客户端移动结束后获取客户端位置，然后使用 RPC 同步到服务器

## RPC 执行问题

参考下面两篇文档

> [Actor Owner and Owning Connection in Unreal Engine | Unreal Engine 5.7 Documentation | Epic Developer Community](https://dev.epicgames.com/documentation/unreal-engine/actor-owner-and-owning-connection-in-unreal-engine)
>
> [Remote Procedure Calls in Unreal Engine | Unreal Engine 5.7 Documentation | Epic Developer Community](https://dev.epicgames.com/documentation/unreal-engine/remote-procedure-calls-in-unreal-engine)

注意点

- Client RPC 可能再服务器上执行
- Multicast RPC 服务器和客户端都会执行
- 客户端调用 Server RPC 时，如果没有 Actor 的 Owning Connection 不是调用的客户端，服务端不会执行。

## Sprite 发光问题

关闭自动曝光，参考

> [UE 2D游戏配置记录 - 哔哩哔哩](https://www.bilibili.com/opus/1154663719496581121?from=search&spm_id_from=333.337.0.0)

